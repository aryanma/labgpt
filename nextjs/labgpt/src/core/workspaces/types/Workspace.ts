export type Workspace = {
    id: string;
    name: string;
    description: string;
    created_by: string;
    created_at: string;
}

export type Paper = {
    id: string;
    title: string;
    date_added: string;
}