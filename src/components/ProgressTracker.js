import React, { useState } from 'react';

// Track reading progress for each paper
const ProgressTracker = ({ paper }) => {
    const [progress, setProgress] = useState(0);
    // Track scroll position in PDF
};

export default ProgressTracker; 