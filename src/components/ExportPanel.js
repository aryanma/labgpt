// Add ability to export notes as PDF/Markdown
const handleExport = (format) => {
    const notes = paperNotes[selectedPaper.id];
    if (format === 'pdf') {
        // Export as PDF
    } else if (format === 'markdown') {
        // Export as MD
    }
}; 